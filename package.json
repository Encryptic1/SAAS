{
  "name": "market-standard",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:local": "pnpm db:setup && cross-env NEXT_PUBLIC_LOCAL_DEV=true LOCAL_DEV=true concurrently -k -n db,apps -c blue,green \"pnpm db:server\" \"wait-on -t 30000 http://127.0.0.1:4000/health && cross-env NEXT_PUBLIC_LOCAL_DEV=true LOCAL_DEV=true turbo run dev --concurrency=12 --filter=standard-polls --filter=standard-proof --filter=standard-metrics --filter=standard-hook --filter=standard-release --filter=standard-vault --filter=standard-links --filter=standard-snippets --filter=standard-status --filter=standard-regex --filter=standard-postmortem\"",
    "db:setup": "tsx scripts/sync-local-env.ts && pnpm --filter @market-standard/db db:setup",
    "db:server": "cross-env LOCAL_DEV=true NEXT_PUBLIC_LOCAL_DEV=true tsx scripts/db-gateway.ts",
    "vercel:setup-envs": "tsx scripts/setup-vercel-envs.ts",
    "smoke:local": "powershell -ExecutionPolicy Bypass -File scripts/smoke-local.ps1",
    "test:e2e": "playwright test",
    "test:e2e:local": "pnpm db:setup && playwright test",
    "typecheck:e2e": "tsc -p tsconfig.e2e.json --noEmit",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.3",
    "@hono/node-server": "^1.13.7",
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.10.2",
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "hono": "^4.6.12",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "wait-on": "^8.0.1"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "overrides": {
      "@electric-sql/pglite": "0.5.3"
    }
  }
}
